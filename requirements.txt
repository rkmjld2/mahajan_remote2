streamlit>=1.38.0
groq>=0.11.0,<0.12.0          # or whatever version you were using
requests>=2.32.0
httpx==0.27.2                 # ← this is the key line – pins to last version before breaking change
